# Setup
1. Install packages as per requirements.txt (venv recommended)
2. Set GEMINI_API_KEY environment variable before running
    1. For VSCode, copy .env.template to .env and set your Gemini API key in .env